# IonImplationEtherCAT - 코드 문서화

## 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [폴더 및 파일 구조](#3-폴더-및-파일-구조)
4. [핵심 컴포넌트 상세 분석](#4-핵심-컴포넌트-상세-분석)
5. [장비 모델 클래스](#5-장비-모델-클래스)
6. [명령 실행 시스템](#6-명령-실행-시스템)
7. [하드웨어 추상화 계층](#7-하드웨어-추상화-계층)
8. [공정 파라미터 시스템](#8-공정-파라미터-시스템)
9. [데이터 영속성 서비스](#9-데이터-영속성-서비스)
10. [자동화 워크플로우](#10-자동화-워크플로우)
11. [UI 컴포넌트](#11-ui-컴포넌트)
12. [좌표계 및 위치 매핑](#12-좌표계-및-위치-매핑)
13. [데이터 흐름](#13-데이터-흐름)
14. [설계 패턴](#14-설계-패턴)
15. [빌드 및 실행](#15-빌드-및-실행)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 정보

| 항목 | 내용 |
|------|------|
| **프로젝트명** | IonImplationEtherCAT |
| **플랫폼** | C# WinForms |
| **프레임워크** | .NET Framework 4.7.2 |
| **용도** | 이온주입 반도체 장비 제어 |
| **하드웨어 통신** | EtherCAT 프로토콜 (IEG3268_Dll) |

### 1.2 시스템 목적

이 애플리케이션은 반도체 제조 공정에서 사용되는 이온주입 장비를 제어합니다. 주요 기능:

- **웨이퍼 관리**: FOUP(Front Opening Unified Pod)에서 웨이퍼 로드/언로드
- **웨이퍼 이송**: Transfer Module(로봇 암)을 통한 자동 이송
- **공정 수행**: 3개의 Process Module에서 이온주입/어닐링 공정
- **실시간 모니터링**: 공정 파라미터 및 상태 실시간 표시

### 1.3 장비 구성

```
┌─────────────────────────────────────────────────────────────┐
│                     장비 레이아웃 (상면도)                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│     [FOUP A]                              [FOUP B]           │
│       (-50°)                               (230°)            │
│         │                                    │               │
│         │         ┌─────────┐                │               │
│         └─────────┤   TM    ├────────────────┘               │
│                   │ (로봇암) │                                │
│         ┌─────────┤  중앙   ├─────────┐                      │
│         │         └─────────┘         │                      │
│         │                             │                      │
│      [PM1]         [PM2]           [PM3]                     │
│       (0°)         (90°)           (180°)                    │
│    이온주입      이온주입         어닐링                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

**구성 요소:**

| 구성요소 | 역할 | 수량 |
|----------|------|------|
| **FOUP A** | 입력 웨이퍼 저장 (5슬롯) | 1개 |
| **FOUP B** | 출력 웨이퍼 저장 (5슬롯) | 1개 |
| **TM (Transfer Module)** | 웨이퍼 이송 로봇 암 | 1개 |
| **PM1 (Process Module 1)** | 이온주입 공정 | 1개 |
| **PM2 (Process Module 2)** | 이온주입 공정 | 1개 |
| **PM3 (Process Module 3)** | 어닐링 공정 | 1개 |

### 1.4 동작 모드

| 모드 | 설명 | 용도 |
|------|------|------|
| **실제 모드 (Real Mode)** | 실제 EtherCAT 하드웨어 제어 | 생산 환경 |
| **시뮬레이션 모드 (Simulation Mode)** | UI 애니메이션만 동작, 하드웨어 없음 | 개발/테스트 환경 |

---

## 2. 시스템 아키텍처

### 2.1 계층형 아키텍처

```
┌──────────────────────────────────────────────────────────────┐
│                    Presentation Layer                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐        │
│  │MainForm  │ │MainView  │ │AlarmView │ │RecipeView│        │
│  │(메인창)   │ │(장비제어) │ │(알람관리) │ │(레시피)  │        │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘        │
├──────────────────────────────────────────────────────────────┤
│                    Business Logic Layer                       │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐         │
│  │ProcessModule │ │TransferModule│ │    Foup      │         │
│  │(공정 챔버)    │ │(로봇 암)      │ │(웨이퍼 용기) │         │
│  └──────────────┘ └──────────────┘ └──────────────┘         │
├──────────────────────────────────────────────────────────────┤
│                    Command Execution Layer                    │
│  ┌──────────────────────────────────────────────────┐       │
│  │              CommandQueue                          │       │
│  │  - 비동기 명령 실행    - 실제/시뮬레이션 분기       │       │
│  │  - 순차적 처리         - 타이밍 제어               │       │
│  └──────────────────────────────────────────────────┘       │
├──────────────────────────────────────────────────────────────┤
│                 Hardware Abstraction Layer                    │
│  ┌────────────────────────────────────────────────────┐     │
│  │              IEtherCATController (인터페이스)        │     │
│  │   ┌─────────────────┐  ┌─────────────────────┐    │     │
│  │   │RealEtherCAT     │  │SimulationEtherCAT   │    │     │
│  │   │Controller       │  │Controller           │    │     │
│  │   │(실제 하드웨어)   │  │(시뮬레이션)          │    │     │
│  │   └─────────────────┘  └─────────────────────┘    │     │
│  └────────────────────────────────────────────────────┘     │
├──────────────────────────────────────────────────────────────┤
│                    Support Services                           │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐         │
│  │ LogManager   │ │RecipeManager │ │HardwarePos.  │         │
│  │ (로깅)       │ │(레시피 저장) │ │Map (위치상수)│         │
│  └──────────────┘ └──────────────┘ └──────────────┘         │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 클래스 의존성 다이어그램

```
MainForm
├── MainView ─────────────────────────────────────────┐
│   ├── ProcessModule (A, B, C)                       │
│   │   ├── ProcessParameters                         │
│   │   ├── Wafer (CurrentWafer)                      │
│   │   ├── IonImplantRecipe                          │
│   │   └── AnnealingRecipe                           │
│   ├── Foup (A, B)                                   │
│   │   └── Wafer[] (WaferSlots)                      │
│   ├── TransferModule                                │
│   │   └── Wafer (CurrentWafer)                      │
│   ├── TMGraphicsPanel                               │
│   │   └── TransferModule (참조)                     │
│   └── CommandQueue                                  │
│       └── ProcessCommand                            │
├── AlarmView                                         │
│   └── LogManager (로그 읽기)                        │
├── RecipeView                                        │
│   └── RecipeSet                                     │
│       ├── IonImplantRecipe                          │
│       └── AnnealingRecipe                           │
└── LogView                                           │
    └── LogManager (로그 표시)                        │
                                                      │
IEtherCATController ◄─────────────────────────────────┘
├── RealEtherCATController (IEG3268 DLL 사용)
└── SimulationEtherCATController
```

---

## 3. 폴더 및 파일 구조

### 3.1 프로젝트 구조

```
IonImplationEtherCAT/
│
├── 📄 IonImplationEtherCAT.sln          # 솔루션 파일
├── 📄 IonImplationEtherCAT.csproj       # 프로젝트 파일
│
├── 📂 Forms/                            # Windows Forms
│   ├── MainForm.cs                      # 메인 윈도우
│   ├── MainForm.Designer.cs             # 메인 윈도우 디자이너
│   └── ManualControlForm.cs             # 수동 제어 폼
│
├── 📂 Views/                            # 사용자 컨트롤 (뷰)
│   ├── MainView.cs                      # 장비 제어 메인 뷰
│   ├── MainView.Designer.cs
│   ├── AlarmView.cs                     # 알람 관리 뷰
│   ├── AlarmView.Designer.cs
│   ├── RecipeView.cs                    # 레시피 관리 뷰
│   ├── RecipeView.Designer.cs
│   ├── LogView.cs                       # 로그 뷰
│   └── LogView.Designer.cs
│
├── 📂 Models/                           # 데이터 모델
│   ├── ProcessModule.cs                 # 공정 챔버 모델
│   ├── TransferModule.cs                # 로봇 암 모델
│   ├── Foup.cs                          # FOUP 컨테이너
│   ├── Wafer.cs                         # 웨이퍼 모델
│   ├── ProcessParameters.cs             # 공정 파라미터
│   ├── IonImplantRecipe.cs             # 이온주입 레시피
│   ├── AnnealingRecipe.cs              # 어닐링 레시피
│   ├── RecipeSet.cs                    # 레시피 세트
│   └── LogEntry.cs                     # 로그 엔트리
│
├── 📂 Commands/                         # 명령 시스템
│   ├── CommandQueue.cs                  # 명령 큐 관리
│   ├── ProcessCommand.cs               # 명령 정의
│   └── CommandType.cs                  # 명령 타입 열거형
│
├── 📂 Controllers/                      # 하드웨어 컨트롤러
│   ├── IEtherCATController.cs          # 컨트롤러 인터페이스
│   ├── RealEtherCATController.cs       # 실제 하드웨어 구현
│   └── SimulationEtherCATController.cs # 시뮬레이션 구현
│
├── 📂 Services/                         # 서비스
│   ├── LogManager.cs                    # 로그 관리 (싱글톤)
│   └── RecipeManager.cs                # 레시피 관리 (정적)
│
├── 📂 Graphics/                         # 그래픽 컴포넌트
│   └── TMGraphicsPanel.cs              # TM 시각화 패널
│
├── 📂 Constants/                        # 상수
│   ├── HardwarePositionMap.cs          # 하드웨어 위치 상수
│   └── TowerLampState.cs               # 타워 램프 상태
│
├── 📂 bin/                             # 빌드 출력
│   ├── Debug/
│   └── Release/
│
├── 📂 Logs/                            # 로그 파일 (런타임 생성)
│   └── log_YYYY-MM-DD.csv
│
├── 📂 Recipes/                         # 레시피 파일 (런타임 생성)
│   └── *.json
│
└── 📄 CLAUDE.md                        # 프로젝트 가이드
```

### 3.2 주요 파일별 라인 수 및 역할

| 파일 | 라인 수 | 주요 역할 |
|------|--------|----------|
| MainView.cs | 1,600+ | 장비 제어 UI, 자동화 워크플로우 |
| CommandQueue.cs | 700+ | 비동기 명령 실행 시스템 |
| ProcessParameters.cs | 540 | 공정 파라미터 애니메이션 |
| TransferModule.cs | 510 | 로봇 암 상태 및 애니메이션 |
| ProcessModule.cs | 460 | 공정 챔버 상태 관리 |
| LogManager.cs | 400 | 로깅 시스템 |
| MainForm.cs | 300 | 메인 윈도우 및 인증 |
| RealEtherCATController.cs | 250+ | 실제 하드웨어 제어 |
| TMGraphicsPanel.cs | 200+ | TM 그래픽 렌더링 |

---

## 4. 핵심 컴포넌트 상세 분석

### 4.1 MainForm.cs - 메인 윈도우

#### 4.1.1 역할
애플리케이션의 진입점이자 인증 게이트웨이입니다.

#### 4.1.2 주요 정적 속성

```csharp
public static bool IsLogined { get; set; }       // 로그인 상태
public static bool IsConnected { get; set; }     // 하드웨어 연결 상태
public static bool IsSimulationMode { get; set; } // 시뮬레이션 모드 여부
```

#### 4.1.3 주요 메서드

| 메서드 | 기능 |
|--------|------|
| `btnLogin_Click()` | 로그인 검증 (admin/1234) |
| `btnConnect_Click()` | 하드웨어 연결 시도 |
| `ShowView(UserControl)` | 뷰 전환 |
| `ActivateAllButtons()` | 버튼 활성화/비활성화 |
| `UpdateStatusPanelColors()` | 연결 상태 시각적 표시 |

#### 4.1.4 하드웨어 연결 흐름

```
[Connect 버튼 클릭]
         │
         ▼
┌─────────────────────────────────────┐
│ IEG3268.CIFX_50RE_Connect() 호출     │
└─────────────────────────────────────┘
         │
    ┌────┴────┐
    │         │
 성공        실패
    │         │
    ▼         ▼
┌─────────┐ ┌─────────────────┐
│Real     │ │Simulation       │
│EtherCAT │ │EtherCATController│
│Controller│ │생성             │
│생성     │ └─────────────────┘
└─────────┘        │
    │              │
    ▼              ▼
IsSimulationMode  IsSimulationMode
= false           = true
```

### 4.2 MainView.cs - 장비 제어 인터페이스

#### 4.2.1 역할
장비 전체를 제어하는 핵심 인터페이스입니다. 1,600줄 이상의 코드로 가장 복잡한 컴포넌트입니다.

#### 4.2.2 주요 필드

```csharp
// 장비 모델
private ProcessModule processModuleA;    // PM1 (이온주입)
private ProcessModule processModuleB;    // PM2 (이온주입)
private ProcessModule processModuleC;    // PM3 (어닐링)
private Foup foupA;                      // 입력 FOUP
private Foup foupB;                      // 출력 FOUP
private TransferModule transferModule;   // 로봇 암

// 제어
private CommandQueue commandQueue;       // 명령 큐
private IEtherCATController etherCATController; // 하드웨어 컨트롤러

// UI
private TMGraphicsPanel tmGraphicsPanel; // TM 그래픽 패널

// 타이머
private Timer processTimer;       // 1초 주기 - 공정 업데이트
private Timer tmAnimationTimer;   // 16ms 주기 - 60 FPS 애니메이션
private Timer lampBlinkTimer;     // 500ms 주기 - 램프 깜빡임
```

#### 4.2.3 타이머 시스템

| 타이머 | 주기 | 역할 |
|--------|------|------|
| **processTimer** | 1,000ms | 공정 상태 업데이트, 파라미터 애니메이션, 진행률 표시 |
| **tmAnimationTimer** | 16ms | TM 회전/암 확장 애니메이션 (약 60 FPS) |
| **lampBlinkTimer** | 500ms | PM 램프 깜빡임 (언로드 요청 시) |

#### 4.2.4 주요 메서드

**UI 업데이트 메서드:**

| 메서드 | 역할 |
|--------|------|
| `UpdateProcessDisplay()` | PM 상태, 진행바, 램프 상태 갱신 |
| `UpdateAllPMParameters()` | 온도, 압력, HV, Dose 표시 갱신 |
| `UpdateFoupADisplay()` / `UpdateFoupBDisplay()` | FOUP 슬롯 상태 표시 |
| `UpdateTowerLamp(state)` | 타워 램프 색상 변경 |

**워크플로우 메서드:**

| 메서드 | 역할 |
|--------|------|
| `StartFullAutomatedWorkflow()` | 전체 자동화 워크플로우 시작 |
| `LoadRecipesFromRecipeView()` | RecipeView에서 레시피 로드 |
| `InitializeHardwareAsync()` | 하드웨어 초기화 시퀀스 |
| `ResetAllState()` | 전체 상태 초기화 |

**웨이퍼 이송 메서드:**

| 메서드 | 역할 |
|--------|------|
| `TransferWaferFromFoupAToPM()` | FOUP A → PM 이송 |
| `TransferWaferFromPMToPM()` | PM → PM 이송 |
| `TransferWaferFromPMToFoupB()` | PM → FOUP B 이송 |

---

## 5. 장비 모델 클래스

### 5.1 ProcessModule.cs - 공정 챔버

#### 5.1.1 상태 다이어그램

```
                    ┌─────────┐
                    │  Idle   │◄──────────────────────┐
                    └────┬────┘                       │
                         │ StartProcess()             │
                         ▼                            │
                    ┌─────────┐                       │
           ┌───────►│ Running │◄──────┐              │
           │        └────┬────┘       │              │
           │             │            │              │
    ResumeProcess()  PauseProcess()   │              │
           │             │            │              │
           │             ▼            │              │
           │        ┌─────────┐       │              │
           └────────┤ Paused  ├───────┘              │
                    └────┬────┘                      │
                         │                           │
                    StopProcess()          CompleteStop()
                         │                           │
                         ▼                           │
                    ┌──────────┐     ┌─────────┐     │
                    │ Stopping ├────►│ Stoped  ├─────┘
                    └──────────┘     └─────────┘
                                          │
                                  하드웨어 정리 완료
                                          │
                         ┌─────────┐      │
                         │  Error  │◄─────┘
                         └─────────┘
                        (예외 상황)
```

#### 5.1.2 모듈 타입

```csharp
public enum ModuleType
{
    PM1,    // 이온주입 공정
    PM2,    // 이온주입 공정
    PM3     // 어닐링 공정
}
```

#### 5.1.3 주요 속성

| 속성 | 타입 | 설명 |
|------|------|------|
| `ModuleState` | State | 현재 상태 (Idle, Running, Paused, 등) |
| `Type` | ModuleType | PM1, PM2, PM3 구분 |
| `processTime` | int | 공정 시간 (초, 35~120 범위) |
| `elapsedTime` | int | 경과 시간 (초) |
| `isWaferLoaded` | bool | 웨이퍼 로드 여부 |
| `IsUnloadRequested` | bool | 언로드 요청 플래그 |
| `CompletedTime` | DateTime | 완료 시각 (FIFO 순서용) |
| `CurrentWafer` | Wafer | 현재 로드된 웨이퍼 |
| `Parameters` | ProcessParameters | 공정 파라미터 객체 |
| `IonRecipe` | IonImplantRecipe | 이온주입 레시피 (PM1/PM2) |
| `AnnealRecipe` | AnnealingRecipe | 어닐링 레시피 (PM3) |
| `IsDoorOpen` | bool | 도어 열림 상태 |

#### 5.1.4 공정 생명주기

```csharp
// 1. 공정 시작
public void StartProcess()
{
    InitializeParametersFromRecipe();  // 레시피에서 목표값 로드
    ModuleState = State.Running;
    elapsedTime = 0;
}

// 2. 공정 업데이트 (1초마다 호출)
public void UpdateProcess(int seconds)
{
    if (ModuleState != State.Running) return;

    elapsedTime += seconds;
    Parameters.UpdatePhased(elapsedTime, processTime, Type);
    LogProcessStep();  // 단계별 로그

    // 공정 완료 확인
    if (elapsedTime >= processTime)
    {
        ModuleState = State.Idle;
        IsUnloadRequested = true;
        CompletedTime = DateTime.Now;
        Parameters.StartFalling();
    }
}

// 3. 공정 정지
public void StopProcess()
{
    ModuleState = State.Stopping;
    Parameters.StartFalling();
}

// 4. 정지 완료 (하드웨어 정리 후)
public void CompleteStop()
{
    ModuleState = State.Idle;
}
```

#### 5.1.5 공정 단계별 로그 (이온주입 - PM1/PM2)

| 경과 시간 | 단계 | 로그 메시지 |
|-----------|------|-------------|
| 0~1초 | 1 | 챔버 진공 배기 시작 |
| 1~3초 | 2 | 진공 도달 |
| 3~4초 | 3 | 이온소스 ON |
| 4~5초 | 4 | 질량분석기 ON |
| 5~6초 | 5 | 가속전압 인가 |
| 6~8초 | 6 | 고전압 안정화 |
| 8~10초 | 7 | 빔게이트 OPEN, 스캐너 시작 |
| 10초~(종료-10초) | 8 | 이온주입 중... |
| (종료-10초)~(종료-3초) | 9 | 빔게이트 CLOSE |
| (종료-3초)~종료 | 10 | 고전압 OFF, 챔버 벤트 |

### 5.2 TransferModule.cs - 로봇 암

#### 5.2.1 상태 다이어그램

```
     ┌────────────────────────────────────┐
     │                                    │
     ▼                                    │
┌─────────┐  SetTargetRotation()  ┌──────────┐
│  Idle   ├──────────────────────►│ Rotating │
└────┬────┘                       └─────┬────┘
     │                                  │
     │ ExtendArm()                      │ 회전 완료
     ▼                                  │
┌─────────┐                             │
│ Moving  │◄────────────────────────────┘
└────┬────┘
     │
┌────┴─────────────────┐
│                      │
▼                      ▼
┌────────────┐    ┌────────────┐
│PickingWafer│    │PlacingWafer│
└────────────┘    └────────────┘
```

#### 5.2.2 주요 속성

| 속성 | 타입 | 설명 |
|------|------|------|
| `CurrentRotationAngle` | float | 현재 회전 각도 (도) |
| `TargetRotationAngle` | float | 목표 회전 각도 |
| `CurrentExtension` | float | 현재 암 확장 거리 (픽셀) |
| `TargetExtension` | float | 목표 암 확장 거리 |
| `IsArmExtended` | bool | 암 확장 상태 |
| `HasWafer` | bool | 웨이퍼 보유 여부 |
| `CurrentWafer` | Wafer | 현재 잡고 있는 웨이퍼 |

#### 5.2.3 회전 제약 조건

```csharp
// 유효 각도 범위: -55° ~ 305°
private const float MIN_ROTATION = -55f;
private const float MAX_ROTATION = 305f;

// 회전 속도: 90°/초
private const float ROTATION_SPEED = 90f;
```

#### 5.2.4 암 확장

```csharp
// 최대 확장: 90 픽셀
// 확장 속도: 200 픽셀/초
```

#### 5.2.5 이벤트

```csharp
public event Action OnRotationComplete;        // 회전 완료
public event Action OnArmMovementComplete;     // 암 이동 완료
```

### 5.3 Foup.cs - 웨이퍼 컨테이너

#### 5.3.1 구조

```
┌─────────────────────────────┐
│         FOUP (5슬롯)         │
├─────────────────────────────┤
│  Slot 4 (Floor 5) - 최상단   │  ← 마지막 추출
│  Slot 3 (Floor 4)           │
│  Slot 2 (Floor 3)           │
│  Slot 1 (Floor 2)           │
│  Slot 0 (Floor 1) - 최하단   │  ← 먼저 추출 (FIFO)
└─────────────────────────────┘
```

#### 5.3.2 주요 속성/메서드

```csharp
public string Name { get; set; }              // "FOUP A" 또는 "FOUP B"
public Wafer[] WaferSlots { get; }            // 5개 슬롯 배열
public bool IsFull { get; }                   // 모든 슬롯 사용 중
public bool IsEmpty { get; }                  // 모든 슬롯 비어있음
public int WaferCount { get; }                // 웨이퍼 개수

public void LoadWafers();                     // 모든 슬롯 채우기
public void UnloadWafers();                   // 모든 슬롯 비우기
public void LoadWafer(int index, Wafer w);    // 특정 슬롯에 로드
public Wafer UnloadWafer(int index);          // 특정 슬롯에서 언로드
```

### 5.4 Wafer.cs - 웨이퍼

#### 5.4.1 상태

```csharp
public enum WaferState
{
    ProcessNone,           // 미처리
    IonProcessComplete,    // 이온주입 완료
    AnnealingProcessComplete, // 어닐링 완료
    Error                  // 에러
}
```

#### 5.4.2 속성

```csharp
public int WaferId { get; }           // 자동 증가 고유 ID
public WaferState CurrentState { get; set; }
public DateTime CreatedTime { get; }
```

---

## 6. 명령 실행 시스템

### 6.1 CommandQueue.cs - 명령 큐

#### 6.1.1 역할
비동기 순차 명령 실행 시스템입니다. 실제 모드와 시뮬레이션 모드를 자동으로 분기합니다.

#### 6.1.2 아키텍처

```
┌─────────────────────────────────────────────────────────┐
│                    CommandQueue                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────────┐                                     │
│  │ Queue<Command>  │ ←── Enqueue(cmd)                    │
│  └────────┬────────┘                                     │
│           │                                              │
│           ▼                                              │
│  ┌─────────────────┐                                     │
│  │ ExecuteAsync()  │ ←── async/await 기반               │
│  └────────┬────────┘                                     │
│           │                                              │
│           ▼                                              │
│  ┌─────────────────────────────────────────────┐        │
│  │         ExecuteCommandAsync(cmd)             │        │
│  │                                              │        │
│  │   ┌────────────┐     ┌────────────────┐     │        │
│  │   │ Real Mode  │     │ Simulation Mode│     │        │
│  │   │            │     │                │     │        │
│  │   │ Hardware + │     │ Animation only │     │        │
│  │   │ Animation  │     │                │     │        │
│  │   └────────────┘     └────────────────┘     │        │
│  └─────────────────────────────────────────────┘        │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

#### 6.1.3 명령 타입 분류

**시뮬레이션 명령 (UI 애니메이션):**

| 명령 | 설명 |
|------|------|
| `RotateTM` | TM을 지정 각도로 회전 |
| `ExtendArm` | TM 암 확장 |
| `RetractArm` | TM 암 수축 |
| `PickWafer` | 웨이퍼 픽업 |
| `PlaceWafer` | 웨이퍼 배치 |

**데이터 관리 명령:**

| 명령 | 설명 |
|------|------|
| `RemoveWaferFromFoup` | FOUP에서 웨이퍼 데이터 제거 |
| `AddWaferToFoup` | FOUP에 웨이퍼 데이터 추가 |
| `UnloadWaferFromPM` | PM에서 웨이퍼 데이터 제거 |
| `LoadWaferToPM` | PM에 웨이퍼 데이터 추가 |
| `StartProcess` | PM 공정 시작 |

**하드웨어 명령 (실제 모드 전용):**

| 명령 | 설명 |
|------|------|
| `HomeUDAxis` | UD축 원점복귀 |
| `HomeLRAxis` | LR축 원점복귀 |
| `MoveUDAxis` | UD축 이동 |
| `MoveLRAxis` | LR축 이동 |
| `ServoOn` / `ServoOff` | 서보 제어 |
| `ExtendCylinder` / `RetractCylinder` | 실린더 제어 |
| `EnableSuction` / `DisableSuction` | 흡착 제어 |
| `EnableExhaust` / `DisableExhaust` | 배기 제어 |
| `OpenPMDoor` / `ClosePMDoor` | PM 도어 제어 |
| `SetPMLampOn` / `SetPMLampOff` | PM 램프 제어 |

**파라미터 명령:**

| 명령 | 설명 |
|------|------|
| `InitializeParameters` | 파라미터 상승 시작 |
| `WaitForParameterStabilization` | 파라미터 안정화 대기 |

#### 6.1.4 타이밍 상수 (밀리초)

```csharp
// 서보 제어
private const int SERVO_ON_DELAY = 1000;
private const int SERVO_OFF_DELAY = 500;

// 축 이동
private const int AXIS_MOVE_SETTLE_DELAY = 1000;

// PM 도어
private const int PM_DOOR_OPEN_DELAY = 2500;
private const int PM_DOOR_CLOSE_DELAY = 2500;

// TM 실린더
private const int CYLINDER_EXTEND_DELAY = 3000;
private const int CYLINDER_RETRACT_DELAY = 3000;

// 흡착/배기
private const int SUCTION_ENABLE_DELAY = 1500;
private const int SUCTION_DISABLE_DELAY = 1000;
private const int EXHAUST_ENABLE_DELAY = 1000;
private const int EXHAUST_DISABLE_DELAY = 1500;

// TM 암
private const int TM_ARM_EXTEND_DELAY = 1500;
private const int TM_ARM_RETRACT_DELAY = 1500;
private const int TM_SETTLE_DELAY = 1000;
```

#### 6.1.5 실제/시뮬레이션 분기 예시

```csharp
case CommandType.RotateTM:
    var controller = mainView.GetEtherCATController();
    bool isRealMode = controller != null &&
                      !(controller is SimulationEtherCATController);

    if (isRealMode)
    {
        // 실제 모드: 하드웨어 + 애니메이션 병렬 실행
        long lrPosition = GetLRPositionFromAngle(angle);
        Task hardwareTask = controller.MoveLRAxis(lrPosition);
        Task animationTask = WaitForTMRotationComplete();
        await Task.WhenAll(hardwareTask, animationTask);
    }
    else
    {
        // 시뮬레이션 모드: 애니메이션만 실행
        mainView.RotateTM(angle);
        await WaitForTMRotationComplete();
    }
    break;
```

### 6.2 ProcessCommand.cs - 명령 정의

#### 6.2.1 구조

```csharp
public class ProcessCommand
{
    public CommandType Type { get; set; }      // 명령 타입
    public string Description { get; set; }     // 설명 문자열
    public object[] Parameters { get; set; }    // 가변 파라미터
    public bool IsCompleted { get; set; }       // 완료 여부
    public Wafer ResultWafer { get; set; }      // 결과 웨이퍼 (체인용)
}
```

#### 6.2.2 명령 생성 예시

```csharp
// TM 회전 명령
new ProcessCommand(CommandType.RotateTM, "FOUP A로 회전", -50f);

// 웨이퍼 제거 명령
new ProcessCommand(CommandType.RemoveWaferFromFoup, "FOUP A 웨이퍼 제거", foupA, 0);

// UD축 이동 명령
new ProcessCommand(CommandType.MoveUDAxis, "UD축 이동", 125188L);
```

---

## 7. 하드웨어 추상화 계층

### 7.1 IEtherCATController.cs - 인터페이스

#### 7.1.1 인터페이스 정의

```csharp
public interface IEtherCATController
{
    // === 축 제어 ===
    Task<bool> MoveUDAxis(long position);    // UD축 이동
    Task<bool> MoveLRAxis(long position);    // LR축 이동
    Task<bool> HomeUDAxis();                 // UD축 원점복귀
    Task<bool> HomeLRAxis();                 // LR축 원점복귀

    // === 서보 제어 ===
    void SetServoUD(bool on);
    void SetServoLR(bool on);

    // === 실린더/진공 제어 ===
    void ExtendCylinder();
    void RetractCylinder();
    void EnableSuction();
    void DisableSuction();
    void EnableExhaust();
    void DisableExhaust();

    // === PM 제어 ===
    void OpenPMDoor(ProcessModule.ModuleType pm);
    void ClosePMDoor(ProcessModule.ModuleType pm);
    void SetPMLamp(ProcessModule.ModuleType pm, bool on);

    // === 타워 램프 ===
    void SetTowerLamp(TowerLampState state);
    void SetTowerLampIndividual(bool red, bool yellow, bool green);
}
```

### 7.2 RealEtherCATController.cs - 실제 하드웨어

#### 7.2.1 외부 DLL 의존성

```csharp
// IEG3268_Dll.dll 사용
using IEG3268_Dll;
```

#### 7.2.2 DO 채널 매핑

| 채널 | 용도 |
|------|------|
| 0 | 타워 램프 - 빨강 |
| 1 | 타워 램프 - 노랑 |
| 2 | 타워 램프 - 초록 |
| 3 | PM1 램프 |
| 4 | PM1 도어 UP |
| 5 | PM1 도어 DOWN |
| 6 | PM2 램프 |
| 7 | PM2 도어 UP |
| 8 | PM2 도어 DOWN |
| 9 | PM3 램프 |
| 10 | PM3 도어 UP |
| 11 | PM3 도어 DOWN |
| 12 | TM 실린더 전진 |
| 13 | TM 실린더 후진 |
| 14 | TM 흡착 |
| 15 | TM 배기 |

#### 7.2.3 축 설정

| 축 | 번호 | 용도 |
|----|------|------|
| UD | 1 | 수직 이동 |
| LR | 2 | 수평/회전 이동 |

### 7.3 SimulationEtherCATController.cs - 시뮬레이션

#### 7.3.1 특징

- 하드웨어 없이 동작
- 축 이동 시뮬레이션 (50,000 units/50ms)
- 상태 추적 (실린더, 진공 등)
- 시각적 명령은 No-op

---

## 8. 공정 파라미터 시스템

### 8.1 ProcessParameters.cs

#### 8.1.1 추적 파라미터

| 파라미터 | 단위 | 기준값 | 설명 |
|----------|------|--------|------|
| Temperature | °C | 25 | 챔버 온도 |
| Pressure | Torr | 760 | 챔버 압력 (760 = 대기압) |
| HV | kV | 0 | 가속 전압 |
| Dose | ions/cm² | 0 | 누적 주입량 |

#### 8.1.2 애니메이션 단계

```
┌───────────┐     ┌────────┐     ┌──────────┐
│  Rising   │────►│ Stable │────►│ Falling  │
│  (상승)   │     │ (안정)  │     │  (하강)  │
└───────────┘     └────────┘     └──────────┘
     │                │               │
     │                │               │
     ▼                ▼               ▼
목표값까지 상승   Dose 누적,      기준값으로
                랜덤 변동        빠르게 하강
```

#### 8.1.3 단계별 업데이트 (이온주입)

```
시간              동작
─────────────────────────────────────────────
0~3초            진공 펌핑만 (Pressure 감소)
3~5초            이온소스 ON, 완만한 온도 상승
5~8초            HV 상승, 온도 계속 상승
8~10초           파라미터 안정화
10초~(종료-10초)  이온주입, Dose 누적
(종료-10초)~     HV OFF 단계
(종료-5초)~      온도 냉각
(종료-2초)~종료  챔버 벤트 (Pressure → 760)
```

#### 8.1.4 주요 메서드

```csharp
// 상승 단계 시작
public void StartRising()

// 단계별 업데이트 (1초마다 호출)
public void UpdatePhased(int elapsedTime, int totalTime, ModuleType type)

// 하강 단계 시작
public void StartFalling()

// 상태 확인
public bool IsRising { get; }
public bool IsFalling { get; }
public bool IsStable { get; }
```

---

## 9. 데이터 영속성 서비스

### 9.1 LogManager.cs - 로그 관리

#### 9.1.1 특징

- **싱글톤 패턴**: 전역 접근 가능
- **스레드 안전**: lock 사용
- **자동 저장**: 실시간 CSV 저장
- **일별 파일**: `log_YYYY-MM-DD.csv`

#### 9.1.2 로그 카테고리

```csharp
public enum LogCategory
{
    Transfer,   // FOUP ↔ TM ↔ PM 이동
    Process,    // 공정 시작/완료
    Hardware,   // 모터, 도어, 실린더 동작
    System,     // 로그인, 연결 이벤트
    Error,      // 오류
    Warning     // 경고
}
```

#### 9.1.3 주요 메서드

```csharp
// 일반 로그 추가
LogManager.Instance.AddLog(job, description, location, category, isAlarm);

// 간편 로그
LogManager.Instance.Log(description, location, category);

// 알람 추가
LogManager.Instance.Alarm(description, location, job);

// 워닝 추가
LogManager.Instance.Warning(description, location, job, isRestored);

// 알람 복구
LogManager.Instance.RestoreAlarm(alarm);
LogManager.Instance.RestoreAllAlarms();
```

#### 9.1.4 이벤트

```csharp
public event Action<LogEntry> OnLogAdded;      // 새 로그 추가
public event Action<LogEntry> OnAlarmRestored; // 알람 복구
public event Action OnLogsLoaded;              // 파일 로드 완료
```

### 9.2 RecipeManager.cs - 레시피 관리

#### 9.2.1 특징

- **정적 클래스**: 인스턴스 불필요
- **JSON 형식**: JavaScriptSerializer 사용
- **자동 폴더 생성**: `[App]/Recipes/`

#### 9.2.2 레시피 구조

```csharp
// 이온주입 레시피 (PM1/PM2)
public class IonImplantRecipe
{
    public string IonGas { get; set; }
    public string Gas { get; set; }
    public double Dose { get; set; }        // ions/cm²
    public double Voltage { get; set; }     // kV
    public double Current { get; set; }
    public double Motor { get; set; }
    public double ScannerVoltage { get; set; }
    public int ProcessTime { get; set; }    // 초
    public double TargetTemperature { get; set; }
    public double TargetPressure { get; set; }
    public double TargetHV { get; set; }
}

// 어닐링 레시피 (PM3)
public class AnnealingRecipe
{
    public double Vacuum { get; set; }      // Torr
    public double Temperature { get; set; } // °C
    public double TargetTemperature { get; set; }
    public double TargetPressure { get; set; }
}

// 레시피 세트
public class RecipeSet
{
    public IonImplantRecipe PM1Recipe { get; set; }
    public IonImplantRecipe PM2Recipe { get; set; }
    public AnnealingRecipe PM3Recipe { get; set; }
    public string Name { get; set; }
    public DateTime LastModified { get; set; }
}
```

#### 9.2.3 주요 메서드

```csharp
// 저장
RecipeManager.SaveRecipe(recipeSet, fileName);

// 불러오기
RecipeSet recipe = RecipeManager.LoadRecipe(fileName);

// 존재 확인
bool exists = RecipeManager.RecipeExists(fileName);

// 삭제
RecipeManager.DeleteRecipe(fileName);

// 목록 조회
string[] names = RecipeManager.GetAllRecipeNames();
```

---

## 10. 자동화 워크플로우

### 10.1 전체 자동화 워크플로우

#### 10.1.1 처리 흐름 (FOUP A → PM1/PM2 → PM3 → FOUP B)

```
┌─────────────────────────────────────────────────────────────┐
│                    전체 공정 흐름                            │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│    [FOUP A]                                    [FOUP B]      │
│    (입력)                                      (출력)        │
│       │                                           ▲         │
│       │                                           │         │
│       ▼                                           │         │
│   ┌───────┐      ┌───────┐      ┌───────┐       │         │
│   │  PM1  │─────►│  PM3  │──────┤       │───────┘         │
│   │이온주입│      │어닐링  │      │       │                 │
│   └───────┘      └───────┘      │       │                 │
│                       ▲          │       │                 │
│   ┌───────┐          │          │       │                 │
│   │  PM2  │──────────┘          │       │                 │
│   │이온주입│                     │       │                 │
│   └───────┘                     └───────┘                 │
│                                                              │
└─────────────────────────────────────────────────────────────┘

웨이퍼 처리 순서:
1. FOUP A에서 웨이퍼 추출 (슬롯 0부터)
2. PM1 또는 PM2로 이송
3. 이온주입 공정 수행
4. 공정 완료 후 PM3로 이송
5. 어닐링 공정 수행
6. 공정 완료 후 FOUP B로 이송 (첫 번째 빈 슬롯)
```

#### 10.1.2 워크플로우 상태 머신

```csharp
// StartFullAutomatedWorkflow() 의 핵심 로직

while (!isWorkflowCancelled)
{
    // 우선순위 1: PM3 → FOUP B (완료된 웨이퍼 배출)
    if (pm3.IsUnloadRequested && pm3.ModuleState == Idle)
    {
        await TransferPM3ToFoupB();
        continue;
    }

    // 우선순위 2: PM1/PM2 → PM3 (어닐링 대기)
    if (pm3.IsEmpty && pm3.ModuleState == Idle)
    {
        if (pm1.IsUnloadRequested)
        {
            await TransferPM1ToPM3();
            continue;
        }
        if (pm2.IsUnloadRequested)
        {
            await TransferPM2ToPM3();
            continue;
        }
    }

    // 우선순위 3: FOUP A → PM1 (신규 웨이퍼 투입)
    if (foupA.HasWafers && pm1.IsEmpty && pm1.ModuleState == Idle)
    {
        await TransferFoupAToPM1();
        continue;
    }

    // 우선순위 4: FOUP A → PM2
    if (foupA.HasWafers && pm2.IsEmpty && pm2.ModuleState == Idle)
    {
        await TransferFoupAToPM2();
        continue;
    }

    // 종료 조건 확인
    if (foupA.IsEmpty && pm1.IsEmpty && pm2.IsEmpty && pm3.IsEmpty)
    {
        // 모든 웨이퍼가 FOUP B로 이동 완료
        break;
    }

    await Task.Delay(500); // 500ms마다 체크
}
```

### 10.2 웨이퍼 이송 시퀀스

#### 10.2.1 FOUP A → PM1 이송 (실제 모드)

```
단계  명령                          설명
──────────────────────────────────────────────────────
1    ServoOn                       서보 활성화
2    HomeUDAxis                    UD축 원점복귀
3    MoveLRAxis(13226)            FOUP A LR 위치로 이동
4    MoveUDAxis(125188)           슬롯 1 Seating 위치
5    ExtendCylinder               실린더 전진
6    EnableSuction                흡착 ON
7    RemoveWaferFromFoup          데이터 제거
8    PickWafer                    웨이퍼 픽업
9    MoveUDAxis(185188)           Lifted 위치로 상승
10   RetractCylinder              실린더 후진
11   HomeUDAxis                   UD축 원점
12   MoveLRAxis(-59064)           PM1 LR 위치로 이동
13   OpenPMDoor(PM1)              PM1 도어 열기
14   MoveUDAxis(1156931)          PM Lifted 위치
15   ExtendCylinder               실린더 전진
16   MoveUDAxis(806931)           PM Seating 위치
17   DisableSuction               흡착 OFF
18   EnableExhaust                배기 ON
19   DisableExhaust               배기 OFF
20   PlaceWafer                   웨이퍼 배치
21   RetractCylinder              실린더 후진
22   HomeUDAxis                   UD축 원점
23   ClosePMDoor(PM1)             PM1 도어 닫기
24   InitializeParameters         파라미터 초기화
25   WaitForParameterStabilization 안정화 대기
26   StartProcess                 공정 시작
```

### 10.3 타워 램프 상태

| 상태 | 색상 | 의미 |
|------|------|------|
| `Off` | 꺼짐 | 연결 안 됨 |
| `Green` | 초록 | 공정 진행 중 |
| `Yellow` | 노랑 | 대기/준비 완료 |
| `Red` | 빨강 | 에러 발생 |

---

## 11. UI 컴포넌트

### 11.1 TMGraphicsPanel.cs - TM 시각화

#### 11.1.1 특징

- **GDI+ 렌더링**: 커스텀 Paint
- **더블 버퍼링**: 깜빡임 방지
- **투명 배경**: 지원
- **60 FPS**: 부드러운 애니메이션

#### 11.1.2 렌더링 요소

| 요소 | 크기 | 색상 |
|------|------|------|
| ArmHigh | 155×63 | RGB(180,180,180) |
| ArmLow | 157×63 | RGB(180,180,180) |
| Bottom (중앙) | 100×100 | RGB(150,150,150) |
| Back | 17×63 | RGB(100,100,100) |
| Wafer | 40×10 | RGB(64,150,220) |

#### 11.1.3 좌표 계산

```csharp
// 회전 시 모든 요소가 Bottom (중앙) 기준으로 회전
// BottomPosition: (287, 211)

private void UpdatePositions()
{
    double radians = CurrentRotationAngle * Math.PI / 180.0;

    // ArmHigh 위치 계산
    ArmHighPosition = new PointF(
        BottomPosition.X + (float)(initialOffset.X * Math.Cos(radians)),
        BottomPosition.Y + (float)(initialOffset.X * Math.Sin(radians))
    );
    // ... 나머지 요소들도 동일한 방식
}
```

### 11.2 뷰 구성

| 뷰 | 파일 | 역할 |
|----|------|------|
| MainView | MainView.cs | 장비 제어, 자동화 워크플로우 |
| AlarmView | AlarmView.cs | 알람/워닝 목록, 복구 기능 |
| RecipeView | RecipeView.cs | 레시피 편집, 저장/불러오기 |
| LogView | LogView.cs | 전체 로그 표시, 필터링 |

---

## 12. 좌표계 및 위치 매핑

### 12.1 UI 각도 (TM 회전)

| 위치 | 각도 |
|------|------|
| FOUP A | -50° |
| PM1 | 0° |
| PM2 | 90° |
| PM3 | 180° |
| FOUP B | 230° |

### 12.2 하드웨어 위치 (모터 단위)

#### FOUP A/B 위치

```csharp
// LR축 위치
LR_FOUP_A = 13226
LR_FOUP_B = -395219

// UD축 위치 (FOUP A와 B 동일)
// Seating (웨이퍼 착좌 위치)
FOUP_UD_SEATING = [125188, 789441, 1482288, 2143669, 2833894]
//                Floor 1  Floor 2  Floor 3  Floor 4  Floor 5

// Lifted (픽업 후 상승 위치)
FOUP_UD_LIFTED = [185188, 901866, 1622288, 2323699, 3014110]
```

#### PM 위치

```csharp
// LR축 위치
LR_PM1 = -59064
LR_PM2 = -190823
LR_PM3 = -322000

// UD축 위치 (모든 PM 동일)
PM_UD_SEATING = 806931    // 웨이퍼 착좌
PM_UD_LIFTED = 1156931    // 픽업 전 상승
```

### 12.3 각도-위치 매핑

```csharp
// CommandQueue.GetLRPositionFromAngle() 사용
private long GetLRPositionFromAngle(float angle)
{
    const float tolerance = 5f;

    if (Math.Abs(angle - (-50f)) < tolerance) return LR_FOUP_A;
    if (Math.Abs(angle - 0f) < tolerance) return LR_PM1;
    if (Math.Abs(angle - 90f) < tolerance) return LR_PM2;
    if (Math.Abs(angle - 180f) < tolerance) return LR_PM3;
    if (Math.Abs(angle - 230f) < tolerance) return LR_FOUP_B;

    return LR_PM1; // 기본값
}
```

---

## 13. 데이터 흐름

### 13.1 웨이퍼 데이터 흐름

```
FOUP A                TM               PM               FOUP B
[Wafer]               │                │                  │
   │                  │                │                  │
   │ UnloadWafer()    │                │                  │
   ├─────────────────►│                │                  │
   │                  │ CurrentWafer   │                  │
   │                  │                │                  │
   │                  │ PlaceWafer()   │                  │
   │                  ├───────────────►│                  │
   │                  │                │ CurrentWafer     │
   │                  │                │ (공정 수행)       │
   │                  │                │                  │
   │                  │ PickWafer()    │                  │
   │                  │◄───────────────┤                  │
   │                  │ CurrentWafer   │                  │
   │                  │                │                  │
   │                  │                │     LoadWafer()  │
   │                  ├───────────────────────────────────►│
   │                  │                │                [Wafer]
```

### 13.2 명령 실행 흐름

```
MainView                 CommandQueue              Controller
   │                          │                        │
   │ Enqueue(commands)        │                        │
   ├─────────────────────────►│                        │
   │                          │                        │
   │ ExecuteAsync()           │                        │
   ├─────────────────────────►│                        │
   │                          │                        │
   │                          │ ExecuteCommandAsync()  │
   │                          ├────────────────────────►
   │                          │                        │
   │                          │    (하드웨어 동작)     │
   │                          │◄────────────────────────
   │                          │                        │
   │  이벤트 콜백             │                        │
   │◄─────────────────────────┤                        │
   │                          │                        │
```

### 13.3 이벤트 기반 통신

```csharp
// LogManager 이벤트
LogManager.Instance.OnLogAdded += (entry) => UpdateLogDisplay();
LogManager.Instance.OnAlarmRestored += (alarm) => RefreshAlarmList();

// TransferModule 이벤트
transferModule.OnRotationComplete += () => HandleRotationComplete();
transferModule.OnArmMovementComplete += () => HandleArmComplete();
```

---

## 14. 설계 패턴

### 14.1 사용된 패턴

| 패턴 | 적용 위치 | 설명 |
|------|----------|------|
| **Singleton** | LogManager | 전역 로깅 서비스 |
| **Strategy** | IEtherCATController | 실제/시뮬레이션 전환 |
| **State** | ProcessModule, TransferModule | 상태 기반 동작 |
| **Observer** | Events (OnLogAdded 등) | 느슨한 결합 |
| **Command** | ProcessCommand, CommandQueue | 명령 캡슐화 |
| **Factory** | CommandQueue 내부 | 명령 타입별 처리 |
| **Repository** | RecipeManager, LogManager | 데이터 저장/조회 |

### 14.2 패턴 적용 예시

#### Singleton (LogManager)

```csharp
public class LogManager
{
    private static readonly Lazy<LogManager> _instance =
        new Lazy<LogManager>(() => new LogManager());

    public static LogManager Instance => _instance.Value;

    private LogManager() { /* 초기화 */ }
}

// 사용
LogManager.Instance.Log("메시지", "위치", LogCategory.System);
```

#### Strategy (IEtherCATController)

```csharp
// 인터페이스 정의
public interface IEtherCATController
{
    Task<bool> MoveUDAxis(long position);
    // ...
}

// 실제 구현
public class RealEtherCATController : IEtherCATController { }

// 시뮬레이션 구현
public class SimulationEtherCATController : IEtherCATController { }

// 사용 (런타임에 전환 가능)
IEtherCATController controller = isRealMode
    ? new RealEtherCATController()
    : new SimulationEtherCATController();
```

---

## 15. 빌드 및 실행

### 15.1 빌드 명령

```bash
# Debug 빌드
msbuild IonImplationEtherCAT.sln /p:Configuration=Debug

# Release 빌드
msbuild IonImplationEtherCAT.sln /p:Configuration=Release
```

### 15.2 실행 요구사항

| 항목 | 요구사항 |
|------|----------|
| OS | Windows 10/11 |
| Framework | .NET Framework 4.7.2 |
| 하드웨어 | EtherCAT 컨트롤러 (실제 모드 시) |
| DLL | IEG3268_Dll.dll (실제 모드 시) |

### 15.3 실행 파일 위치

```
bin\Debug\IonImplationEtherCAT.exe
bin\Release\IonImplationEtherCAT.exe
```

### 15.4 로그인 정보

| 항목 | 값 |
|------|-----|
| ID | admin |
| Password | 1234 |

---

## 부록

### A. 용어 정리

| 용어 | 설명 |
|------|------|
| **FOUP** | Front Opening Unified Pod - 웨이퍼 운반 용기 |
| **TM** | Transfer Module - 로봇 암 |
| **PM** | Process Module - 공정 챔버 |
| **EtherCAT** | 산업용 이더넷 프로토콜 |
| **UD축** | Up-Down 축 (수직 이동) |
| **LR축** | Left-Right 축 (수평/회전) |
| **Seating** | 웨이퍼가 안착되는 위치 |
| **Lifted** | 픽업 전 상승 위치 |
| **HV** | High Voltage - 가속 전압 |
| **Dose** | 이온 주입량 (ions/cm²) |

### B. 에러 처리

| 에러 상황 | 처리 방식 |
|----------|----------|
| 하드웨어 연결 실패 | 시뮬레이션 모드로 전환 |
| 축 이동 실패 | 알람 발생, 워크플로우 중단 |
| 파라미터 안정화 타임아웃 | 경고 후 계속 진행 (60초 제한) |
| 명령 실행 실패 | 에러 메시지 표시, 워크플로우 중단 |

### C. 파일 경로

| 항목 | 경로 |
|------|------|
| 로그 파일 | `[App]\Logs\log_YYYY-MM-DD.csv` |
| 레시피 파일 | `[App]\Recipes\*.json` |
| 기본 레시피 | `[App]\Recipes\default_recipe.json` |

---

*문서 작성일: 2025-12-03*
*문서 버전: 1.0*
